[tool.poetry]
name = "co-dev-backend"
version = "0.1.0"
description = "The backend for co-dev teammates search tool"
authors = []

[tool.poetry.dependencies]
python = "^3.8"
Django = "^4.1.6"
djangorestframework = "^3.14.0"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
